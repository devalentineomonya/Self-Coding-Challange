 // if (array.length === 0) {
    //     return "emptyArray";
    // }

    // for (let i = 0; i < array.length; i++) {
    //     if (array[i] === element) {
    //         return `Element Found at position ${i}`;
    //     }
    // }